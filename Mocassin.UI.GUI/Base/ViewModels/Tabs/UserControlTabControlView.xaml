<UserControl x:Class="Mocassin.UI.GUI.Base.ViewModels.Tabs.UserControlTabControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mocassin.UI.GUI.Base.ViewModels.Tabs"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:UserControlTabControlViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800"
             Height="Auto" Width="Auto">

  <Grid>
    <TabControl ItemsSource="{Binding ObservableItems}" SelectedItem="{Binding SelectedTab}">
      <TabControl.ItemTemplate>
        <DataTemplate DataType="{Binding ViewModelType}">
          <Binding Source="UserControl"></Binding>
        </DataTemplate>
      </TabControl.ItemTemplate>
    </TabControl>
   </Grid>
</UserControl>
