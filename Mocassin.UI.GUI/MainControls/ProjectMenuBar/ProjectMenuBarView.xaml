<UserControl x:Class="Mocassin.UI.GUI.MainControls.ProjectMenuBar.ProjectMenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Mocassin.UI.GUI.MainControls.ProjectMenuBar"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:ProjectMenuBarViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid Margin="0,0,0,0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
  </Grid>

  <UserControl.Resources>
    <Style x:Key="Style.DockPanel" TargetType="DockPanel">
      <Setter Property="Width" Value="*" />
      <Setter Property="Height" Value="*" />
    </Style>
  </UserControl.Resources>

</UserControl>
