<UserControl x:Class="Mocassin.UI.GUI.Controls.VisualizerDX.Viewport.DataControl.DX3DViewportSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Mocassin.UI.GUI.Controls.VisualizerDX.Viewport.DataControl"
             xmlns:viewport="clr-namespace:Mocassin.UI.GUI.Controls.VisualizerDX.Viewport"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewport:DX3DViewportViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="White">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="10" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="MSAA Level" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="2" ItemsSource="{Binding MsaaLevels}" SelectedItem="{Binding MsaaLevel}">
        </ComboBox>
      </Grid>
    </ScrollViewer>
  </Grid>
</UserControl>