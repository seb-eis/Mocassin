<UserControl
  x:Class="Mocassin.UI.GUI.Controls.ProjectWorkControl.ModelControls.ModelCustomization.GridControl.KineticRuleSetControlView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Mocassin.UI.GUI.Controls.ProjectWorkControl.ModelControls.ModelCustomization.GridControl"
  xmlns:views="clr-namespace:Mocassin.UI.GUI.Base.Views"
  mc:Ignorable="d"
  d:DataContext="{d:DesignInstance local:KineticRuleSetControlViewModel, IsDesignTimeCreatable=True}"
  d:DesignHeight="450" d:DesignWidth="800"
  Background="Transparent">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <ComboBox Grid.Row="0" ItemsSource="{Binding DataCollection}"
              SelectedItem="{Binding SelectedCollectionItem}"
              Margin="{StaticResource App.Margin.VerticalStackLarge}"
              DisplayMemberPath="Name"
              ToolTip="Search rule entry"
              IsTextSearchEnabled="True"
              IsEditable="True"/>

    <Border Grid.Row="1" Margin="{StaticResource App.Margin.VerticalStackLarge}">
      <TextBlock TextWrapping="Wrap" TextAlignment="Justify">
        <Run Text="Description:" />
        <LineBreak/>
        <Run Text="Auto-generated transition rule for the" />
        <Run Text="{Binding KineticRuleSet.Transition.Name, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
        <Run Text="transition" />
        <Run Text="set. State change behavior is flagged with" />
        <Run Text="{Binding SelectedCollectionItem.RuleFlags, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
        <Run Text="and describes" />
        <Run Text="{Binding SelectedCollectionItem.DependencyRuleCount, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
        <Run Text="hidden rule(s) that will receive the same settings." />
        <Run Text="The reference state change behavior is:" />
        <LineBreak/>
        <Run Text="(State-0) &#x2794;" />
        <Run Text="{Binding SelectedCollectionItem.StartState, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
        <LineBreak/>
        <Run Text="(State-1) &#x2794;" />
        <Run Text="{Binding SelectedCollectionItem.TransitionState, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
        <LineBreak/>
        <Run Text="(State-2) &#x2794;" />
        <Run Text="{Binding SelectedCollectionItem.FinalState, Mode=OneWay}"
             Style="{StaticResource App.Style.Run.Keyword}" />
      </TextBlock>
    </Border>

    <DataGrid Name="PermutationsDataGrid" Grid.Row="2"
              ItemsSource="{Binding DataCollection}"
              SelectedItem="{Binding SelectedCollectionItem}"
              ColumnWidth="*"
              CanUserDeleteRows="False"
              CanUserAddRows="False"
              AutoGenerateColumns="False">
      <DataGrid.RowHeaderTemplate>
        <DataTemplate>
          <views:ObjectRowHeaderLogo DataContext="D" />
        </DataTemplate>
      </DataGrid.RowHeaderTemplate>

      <DataGrid.Columns>
        <DataGridTextColumn Binding="{Binding Name}" Header="Name"/>
        <DataGridTextColumn Binding="{Binding DependencyRuleCount}" Header="Hidden rules"
                            IsReadOnly="True"/>
        <DataGridTextColumn
          Binding="{Binding AttemptFrequency, Converter={StaticResource App.Converter.NumericExpressionToValue}}"
          Header="Attempt frequency [Hz]" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>