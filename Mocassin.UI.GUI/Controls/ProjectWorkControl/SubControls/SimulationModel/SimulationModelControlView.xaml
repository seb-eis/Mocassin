<UserControl
  x:Class="Mocassin.UI.GUI.Controls.ProjectWorkControl.SubControls.SimulationModel.SimulationModelControlView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Mocassin.UI.GUI.Controls.ProjectWorkControl.SubControls.SimulationModel"
  xmlns:dataControl="clr-namespace:Mocassin.UI.GUI.Controls.ProjectWorkControl.SubControls.SimulationModel.DataControl"
  d:DataContext="{d:DesignInstance local:SimulationModelControlViewModel, IsDesignTimeCreatable=True}"
  mc:Ignorable="d"
  d:DesignHeight="450" d:DesignWidth="800"
  Background="Transparent">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition
        Height="*" />
      <RowDefinition
        Height="*" />
    </Grid.RowDefinitions>

    <GroupBox
      Grid.Row="0"
      Header="[MMC] Metropolis Simulation Bases">

      <dataControl:MetropolisSimulationControlView
        DataContext="{Binding MetropolisSimulationViewModel}" />

    </GroupBox>

    <GroupBox
      Grid.Row="1"
      Header="[KMC] Kinetic Simulation Bases">

      <dataControl:KineticSimulationControlView
        DataContext="{Binding KineticSimulationViewModel}" />

    </GroupBox>
  </Grid>
</UserControl>
