<UserControl x:Class="Mocassin.UI.GUI.Controls.ProjectMenuBar.SubControls.ControlMenu.ControlMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:Mocassin.UI.GUI.Controls.ProjectMenuBar.SubControls.ControlMenu"
             xmlns:particleCommands="clr-namespace:Mocassin.UI.GUI.Controls.ProjectMenuBar.SubControls.ControlMenu.Commands"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:ControlMenuViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Menu FontSize="14">
          <MenuItem Header="Model Controls">
            <MenuItem Header="Open Particle Control">
              <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                  <i:InvokeCommandAction Command="{Binding CommandSource}"
                                         CommandParameter="{x:Type particleCommands:AddParticleControlTabCommand}"/>
                </i:EventTrigger>
              </i:Interaction.Triggers>
            </MenuItem>
          </MenuItem>
        </Menu>    
    </Grid>
</UserControl>
